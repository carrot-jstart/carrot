name: Publish all Maven Central

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      release_version:
        description: 'Release version to publish, for example 0.9.1. Leave empty to use the version in pom.xml.'
        required: false
        type: string

permissions:
  contents: read

jobs:
  publish:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    env:
      MAVEN_CENTRAL_USERNAME: ${{ secrets.MAVEN_CENTRAL_USERNAME }}
      MAVEN_CENTRAL_PASSWORD: ${{ secrets.MAVEN_CENTRAL_PASSWORD }}
      MAVEN_GPG_PASSPHRASE: ${{ secrets.MAVEN_GPG_PASSPHRASE }}
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Set up JDK 17
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '17'
          cache: maven
          server-id: central
          server-username: MAVEN_CENTRAL_USERNAME
          server-password: MAVEN_CENTRAL_PASSWORD
          gpg-private-key: ${{ secrets.MAVEN_GPG_PRIVATE_KEY }}
          gpg-passphrase: MAVEN_GPG_PASSPHRASE

      - name: Trust imported GPG key
        shell: pwsh
        run: |
          $keys = gpg --list-secret-keys --keyid-format LONG 2>`$null | Select-String '^sec' | ForEach-Object { $_ -replace '.*/([A-F0-9]+).*', '$$1' }
          foreach ($keyId in $keys) {
            Write-Host "Trusting GPG key: $keyId"
            "trust`n5`ny`nquit" | gpg --batch --command-fd 0 --edit-key "$keyId" trust 2>`$null
          }
          Write-Host "Imported GPG secret keys:"
          gpg --list-secret-keys --keyid-format LONG

      - name: Validate release version
        shell: pwsh
        run: |
          $pomFiles = @(
            "spring-cloud-starter-carrot-config/pom.xml",
            "spring-cloud-starter-carrot-discovery/pom.xml",
            "spring-cloud-starter-carrot-scheduling/pom.xml",
            "dubbo-spring-boot-starter/pom.xml"
          )

          $manualVersion = "${{ inputs.release_version }}"
          if ("${{ github.ref_type }}" -eq "tag") {
            $expectedVersion = "${{ github.ref_name }}".TrimStart('v')
            Write-Host "Using tag version: $expectedVersion"
          } elseif (-not [string]::IsNullOrWhiteSpace($manualVersion)) {
            $expectedVersion = $manualVersion.Trim()
            Write-Host "Using manual input version: $expectedVersion"
          } else {
            [xml]$firstPom = Get-Content $pomFiles[0]
            $expectedVersion = $firstPom.project.version
            Write-Host "No tag or manual version provided, using pom.xml version: $expectedVersion"
          }

          foreach ($pomPath in $pomFiles) {
            [xml]$pom = Get-Content $pomPath
            $actualVersion = $pom.project.version
            if ($actualVersion -ne $expectedVersion) {
              throw "Version mismatch in $pomPath. Expected $expectedVersion but found $actualVersion."
            }
          }

      - name: Publish spring-cloud-starter-carrot-config
        run: mvn -B -ntp -Pcentral-release -DskipTests -Dgpg.passphrase="$MAVEN_GPG_PASSPHRASE" deploy -f spring-cloud-starter-carrot-config/pom.xml

      - name: Publish spring-cloud-starter-carrot-discovery
        run: mvn -B -ntp -Pcentral-release -DskipTests -Dgpg.passphrase="$MAVEN_GPG_PASSPHRASE" deploy -f spring-cloud-starter-carrot-discovery/pom.xml

      - name: Publish spring-cloud-starter-carrot-scheduling
        run: mvn -B -ntp -Pcentral-release -DskipTests -Dgpg.passphrase="$MAVEN_GPG_PASSPHRASE" deploy -f spring-cloud-starter-carrot-scheduling/pom.xml

      - name: Publish dubbo-spring-boot-starter
        run: mvn -B -ntp -Pcentral-release -DskipTests -Dgpg.passphrase="$MAVEN_GPG_PASSPHRASE" deploy -f dubbo-spring-boot-starter/pom.xml
